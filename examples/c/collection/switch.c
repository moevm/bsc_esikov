#include <stdio.h>
#include "switch.h"

int updateCriticalNumber(int value)
{
    switch (value)
    {
        case 0:
        {
            int y = 20;
            value = 45 - 21 + func(temp);
            return value;
        }
        case -10:
            return 10;
        case 10:
        {
            return func(value);
            break;
        }
        default:
            return value + 64;
    }
}
