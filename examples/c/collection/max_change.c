#include <stdio.h>

#include <stdlib.h>

#include "max_change.h"



// function
long myFunc(long x)
{
    //double value = 0.456;
    // if
    if (x == 0)
    {
        return x + 64;
                                }
    if (x == -10)

                            return 10;
        if (x == 10)
                    {
            // return
            return func(x);
            }
    return 0;
}
/*
    end file
*/
