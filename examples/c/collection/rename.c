#include <stdio.h>
#include "rename.h"

int changeValue(int number)
{
    if (number == 0) {
        int y = 20;
        value = 45 - 21 + func(temp);
        return value;
    }
    if (number == -10) {
        return 10;
    }
    if (number == 10) {
        return func(number);
    } else {
        return 64 + number;
    }

}
